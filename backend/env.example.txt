# Server Configuration
PORT=3000
NODE_ENV=production

# API Security
# ⚠️ REQUIRED: Generate a secure API key using: node scripts/generate-api-key.js
# The API key must be at least 32 characters long
# Never use default or example values in production
API_KEY=your-secure-api-key-here-change-in-production

# Database Configuration (Supabase / PostgreSQL)
DATABASE_URL=postgresql://user:password@host:port/database

# Example for Supabase:
# DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# ⚠️ SECURITY NOTES:
# 1. Never commit .env files to version control
# 2. Generate a secure API key: node scripts/generate-api-key.js
# 3. Use different API keys for development and production
# 4. Rotate API keys regularly
# 5. Keep API keys secret and never expose them in logs or error messages

